import express from "express";
const router = express.Router();
export default router; 


/** 
   * @desc Show number of users, active users, new signups, etc
   * @route /api/dashboard/users
   * @method GET
   * @access private
   */  




/** 
   * @desc Show total revenue, monthly recurring revenue (MRR), plan distribution
   * @route /api/dashboard/revenue
   * @method GET
   * @access private
   */  




/** 
   * @desc Show stats about product usage, stock, active items, etc.
   * @route  /api/dashboard/products
   * @method GET
   * @access private
   */  




/** 
   * @desc Total orders, pending, completed, canceled, etc.
   * @route  /api/dashboard/orders
   * @method GET
   * @access private
   */  

